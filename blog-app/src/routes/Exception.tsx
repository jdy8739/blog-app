import styled from "styled-components";

const P = styled.p`
	margin-top: 240px;
	text-align: center;
`;

function Exception() {
	return (
		<>
			<P>Cannot find the page. :(</P>
		</>
	);
}

export default Exception;
